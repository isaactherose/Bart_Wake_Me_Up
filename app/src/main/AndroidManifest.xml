<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oneroseapps.bartwakemeup.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.oneroseapps.bartwakemeup.app.SetJourney"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.oneroseapps.bartwakemeup.app.TimeLeftActivity"
            android:label="@string/title_activity_time_left" >
        </activity>
        <activity
            android:name="com.oneroseapps.bartwakemeup.app.WakeUpActivity"
            android:label="@string/title_activity_wake_up" >
        </activity>
    </application>

</manifest>
